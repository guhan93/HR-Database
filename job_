create table job_history1(
emp_id varchar(10),
constraint f50 foreign key(emp_id) references employee1(emp_id),
start_date date,
end_date date,
dept_id varchar(10),
constraint f51 foreign key(dept_id) references department1(dept_id),
desig_id varchar(10),
constraint f52 foreign key(desig_id) references designation1(desig_id));